#include<iostream>
using namespace std;
int main(){
    // PolyMorphism -> Many Forms
    // A function or an operator behaves differently even though the code is same exactly.
    // def: same entity(functions and operators) behaves differently in different situations.




    
    return 0;
}